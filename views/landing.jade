doctype html
html(lang='en')
  include common/header.jade
  +head('Starter MVP')
  body
    header
      include common/navbar.jade
      +navbar(user)
      .container
        .row.align-items-center
          .col-md-6.order-md-1.text-center.text-md-left.pr-md-5
            h1.mb-3 Minimum Viable Landing Page
            p.lead This tutorial teaches you how to build a Node.js web application covering key topics.
            p.lead.mb-4 Learn how to launch a landing page and start collecting sales leads.
            form(action="/", method="POST").input-group.mb-3
              input(name="lead_email", type="email", placeholder="Enter your email").form-control.input-group-append
              button(type="submit").btn.btn-primary Submit
        
    include common/footer.jade
    +footer()
    script(src='/javascripts/leads.js')
